// ADVENTURE
// Expected URL: /?id=[adventure]

// Get URL Params and store variable based on id

// Find matching ID from JSON data and store adventure variable

// Get content div from DOM

// Create and append new form element to div

// For each adventure.choice, make new: 
    // h2 for adventure name with 
    // p for choice.description 
    // Create new label
    // Create new radio input 
        // name = choice (should be the same across all radios) 
        // value = choice.id
    // Set input's textContent = choice.description 

// Append new elements

// On submit, get user's choice (value = id)

// Show results:
    // In results box, set textContent to matching choice.id
    // Show button to head back to map

// Update localStorage to show adventure as completed
